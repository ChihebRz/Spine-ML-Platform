# Core ML & Data
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.2.0
joblib>=1.2.0

# FastAPI backend
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Web / static serving (optional but needed for frontend)
jinja2>=3.1.0   # if you use templates later

# Development & utils
python-multipart>=0.0.5  # for form data if needed later